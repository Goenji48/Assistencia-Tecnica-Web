/*var connection = new ActiveXObject("ADODB.Connection");*/
var password_input = document.getElementById("password_input");
var email_input = document.getElementById("email_input").value;
var confirm_password_input = document.getElementById("confirm_password_input");

v/*ar connectionstring = "driver={sql server};server=QITBLRQIPL030;database=adventureworks;uid=sa;password=12345";

connection.Open(connectionstring);
var rs = new ActiveXObject("ADODB.Recordset");

rs.Open("Select * from table", connection);
rs.MoveFirst
while(!rs.eof){
    document.write(rs.fields(1));
    rs.moveNext;
}

rs.close;
connection.close;*/



function clique(){
    console.log("awkfjawlkfnl");
}
